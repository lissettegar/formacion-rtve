apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: GUID-formacion
  labels:
    app: nginx
spec:
  containers:
    - image: quay.io/jitesoft/nginx
      name: nginx
      ports:
      - containerPort: 80
